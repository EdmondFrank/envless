---
title: Latest release
description: The newest envless release, with notes, performance deltas, and downloads.
---

import ReleaseCard from "../../../components/ReleaseCard.astro";
import { getReleases, getLatestRelease } from "../../../lib/releases.ts";

export const releases = await getReleases();
export const latest = getLatestRelease(releases);

This page always shows the newest published release. The
[Changelog](../changelog/) lists everything; [All versions](../versions/)
is the compact table.

{latest === null ? (
  <p><em>No releases have been published yet.</em></p>
) : (
  <ReleaseCard release={latest} />
)}
